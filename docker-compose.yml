services:
  assistant:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      # SQLite database — persists across restarts
      - ./data:/app/data
      # Google OAuth tokens — contains secrets, never commit this folder
      - ./gogcli-config:/app/gogcli-config:ro
