services:
  assistant:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      # SQLite database — persists across restarts
      - ./data:/app/data
      # Google OAuth tokens are injected via GOG_CREDENTIALS_JSON and GOG_REFRESH_TOKEN_JSON env vars
